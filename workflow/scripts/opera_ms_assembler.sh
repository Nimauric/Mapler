#!/bin/sh
# "$1" : name of the short-reads run (SRA accession number)
# "$2" : path/to/the/R1/short/reads/run.fastq.gz
# "$3" : path/to/the/R2/short/reads/run.fastq.gz

# "$4" : name of the long-reads run (SRA accession number)
# "$5" : path/to/the/long/reads/run.fastq.gz

# "$6" : path/to/the/output/folder

generic_arguments = "--no-ref-clustering"
